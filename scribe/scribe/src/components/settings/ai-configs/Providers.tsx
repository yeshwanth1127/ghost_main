import { UseSettingsReturn } from "@/types";

export const Providers = ({}: UseSettingsReturn) => {
  return (
    <div className="space-y-3">
      {/* Provider configuration removed */}
    </div>
  );
};
