pub mod fs_read;
pub mod fs_write;

// Re-export for convenience (unused but kept for potential future use)
// pub use fs_read::CapabilityScope as FsReadScope;
// pub use fs_write::CapabilityScope as FsWriteScope;
